[build]
  command = "cd packages/server && pnpm install --prod --ignore-scripts --ignore-workspace --no-frozen-lockfile"
  functions = "netlify/functions"
  publish = "netlify/static"

[build.environment]
  NODE_VERSION = "20"

# Route all requests to the Waline comment function
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/comment/:splat"
  status = 200
  force = true